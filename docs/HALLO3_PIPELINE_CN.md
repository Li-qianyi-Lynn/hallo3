# Hallo3 完整训练流程

---

## 大图：目标是什么

```
输入：一张人脸照片 + 一段音频
输出：这个人在说这段话的视频

训练目标：让模型学会"嘴巴怎么动、表情怎么变"
```

---

## 第一步：数据预处理（训练前）

在训练之前，需要把原始视频处理成模型能读的格式：

```
原始视频 (.mp4)
    │
    ├─► 提取人脸 embedding (.pt)   ← 用人脸识别模型，把"这是谁"压缩成一个向量
    ├─► 提取音频 embedding (.pt)   ← 用 wav2vec2，把声音压缩成向量（Stage 2 用）
    ├─► 生成人脸 mask 图           ← 标记出人脸区域在哪（哪些像素是脸）
    └─► 写入 metadata.json         ← 把所有路径和 caption 记录到一个 json 文件
```

`metadata.json` 里每一条长这样：

```json
{
  "video_path": "/data/clip_001.mp4",
  "face_emb_path": "/data/clip_001_face.pt",
  "caption": "a person talking",
  "face_mask_union_path": "/data/clip_001_mask.png"
}
```

---

## 第二步：DataLoader 取数据

```
metadata.json
    │
    ▼
SFTDataset.__getitem__(index)
    │
    ├─ 1. 打开视频，随机选一个起始帧
    │       start = random(0, 视频总帧数 - 49)
    │
    ├─ 2. 连续读取 49 帧  [49, H, W, C]
    │
    ├─ 3. 随机取一帧作为参考图（"这个人长什么样"）
    │
    ├─ 4. 加载人脸 embedding
    │
    ├─ 5. 生成人脸 mask（只保留脸部区域）
    │
    ├─ 6. 缩放到 480×720，归一化到 [-1, 1]
    │
    └─ 返回 dict:
         mp4       → 视频帧  [49, C, H, W]
         ref_image → 参考图  [1, C, H, W]
         face_emb  → 人脸向量
         mask_ref  → 人脸区域图
         txt       → 文本 caption
```

**为什么随机选起始帧？**
每次训练从视频不同位置截取片段，相当于数据增强，让模型见过更多样的动作和表情。

---

## 第三步：模型结构（两个关键组件）

```
┌─────────────────────────────────────┐
│         SATVideoDiffusionEngine     │
│                                     │
│  ┌──────────┐    ┌───────────────┐  │
│  │  VAE     │    │  DiT          │  │
│  │ (压缩器) │    │ (扩散模型主体) │  │
│  └──────────┘    └───────────────┘  │
└─────────────────────────────────────┘
```

### VAE（变分自编码器）— 压缩器

```
视频帧 [49, 3, 480, 720]
    ↓ encode（压缩，缩小 8 倍）
latent [49, 16, 60, 90]     ← 小很多！在这个压缩空间里训练，省显存
    ↓ decode（解压，只在推理时用）
视频帧 [49, 3, 480, 720]
```

> 类比：把一本书压缩成摘要，在摘要层面学习，最后还原成书。
> 好处：显存占用降低 64 倍（8×8 空间压缩），训练速度大幅提升。

### DiT（扩散 Transformer）— 真正学习的部分

- 基础模型是 CogVideoX-5B，一个 50 亿参数的视频生成模型
- 输入：加了噪声的 latent + 条件信息（人脸、音频、文本）
- 输出：预测噪声是什么，从而学会去噪、生成视频

---

## 第四步：训练的核心 — 扩散过程

```
正向过程（加噪，固定的，不需要学习）：
干净的 latent ──加随机噪声──► 纯噪声

反向过程（去噪，模型要学的）：
纯噪声 ──预测并去噪──► 干净的 latent ──VAE解码──► 视频
```

**每一步训练（shared_step）做什么：**

```
1. 视频帧 ──VAE压缩──► latent x         进入潜空间

2. 人脸图 ──VAE压缩──► 参考 latent      人脸条件（告诉模型"这个人长什么样"）

3. 给 latent x 随机加 t 步的噪声        模拟扩散过程中的某一时刻

4. DiT 预测：这个噪声是什么？           核心学习任务
   （同时参考：人脸 embedding、音频 embedding、文本）

5. loss = 预测的噪声 vs 真实加的噪声    差越小越好

6. 反向传播 → 更新权重
```

> 类比：老师给作业随机涂改（加噪），学生学会把涂改还原成原始答案（去噪）。
> 学了足够多的涂改样本后，学生就能无中生有地写出正确答案（生成视频）。

---

## 第五步：训练循环（DeepSpeed 管理）

```
training_main() 启动
    │
    └─ 循环 30000 次：
         │
         ├─ 1. DataLoader 取一批数据（8 卡各取 1 个视频，共 8 个）
         │
         ├─ 2. forward_step()
         │       └─ model.shared_step()  ← 算 loss（见第四步）
         │
         ├─ 3. DeepSpeed 自动 backward() ← 反向传播，计算每个参数的梯度
         │
         ├─ 4. gradient clipping = 0.1   ← 梯度太大就截断，防止训练崩溃
         │
         ├─ 5. optimizer.step()          ← 用梯度更新权重（lr = 1e-5，小步慢走）
         │
         ├─ 每 500 步：保存 checkpoint   ← 存档，防止训练中断丢失进度
         └─ 每 30000 步：验证，生成样本视频
```

**多卡是怎么配合的？**

```
GPU 0  GPU 1  GPU 2  GPU 3  GPU 4  GPU 5  GPU 6  GPU 7
  │      │      │      │      │      │      │      │
 视频0  视频1  视频2  视频3  视频4  视频5  视频6  视频7
  │      │      │      │      │      │      │      │
  └──────┴──────┴──────┴──────┴──────┴──────┴──────┘
                         ↓
                   梯度 All-Reduce         ← 8 卡梯度求平均
                         ↓
                   统一更新权重            ← 8 卡权重始终保持一致
```

---

## 第六步：两阶段训练

```
Stage 1：学会"控制人脸外观"
───────────────────────────────────────────────────
输入条件：参考人脸图 + 人脸 embedding + 文本 caption
训练目标：给一张人脸图，能生成这个人说话的视频
侧重点：  长得像不像（人脸一致性）
数据集：  SFTDataset

    ↓ Stage 1 训练完，保存 checkpoint

Stage 2：学会"音频驱动口型"
───────────────────────────────────────────────────
输入条件：Stage 1 所有条件 + 音频 embedding（wav2vec2 提取）
训练目标：让嘴型和音频精确对上，表情跟着语音自然变化
侧重点：  说得像不像（音视频同步）
数据集：  Stage2_SFTDataset

    ↓ Stage 2 训练完，得到最终模型
```

**为什么要分两阶段？**
直接同时学人脸和音频太难，先把人脸学稳定，再在此基础上叠加音频控制，降低训练难度，效果也更好。

---

## 整体流程一张图

```
原始视频
  │
  ▼ 【预处理】
人脸emb + 音频emb + mask + metadata.json
  │
  ▼ 【DataLoader】每次随机取 49 帧片段
batch = {mp4, ref_image, face_emb, mask_ref, txt}
  │
  ▼ 【VAE 编码】压缩到潜空间，节省显存
latent（空间缩小 8 倍）
  │
  ▼ 【加噪声】模拟扩散过程
noisy_latent
  │
  ▼ 【DiT 前向传播】预测噪声
    条件注入：人脸 embedding + 音频 embedding + 文本
predicted_noise
  │
  ▼ 【计算 Loss】
loss = ||predicted_noise - real_noise||²
  │
  ▼ 【DeepSpeed 反向传播】
梯度计算 → gradient clipping → optimizer.step
  │
  重复 30000 次（Stage 1）+ 30000 次（Stage 2）
  │
  ▼
训练完成
  │
  ▼ 【推理】
输入：任意人脸照片 + 任意音频
输出：该人说话的视频
```

---

## 关键数字速查

| 参数 | 值 | 含义 |
|---|---|---|
| train_iters | 30000 × 2 阶段 | 总更新次数 |
| batch size（有效） | 8 | 8 卡 × 每卡 1 个视频 |
| max_num_frames | 49 帧 | 每样本约 6 秒（@ 8fps） |
| video_size | 480 × 720 | 训练分辨率 |
| lr | 1e-5 | 学习率（微调用小值） |
| VAE 压缩比 | 8× | 空间维度缩小 8 倍 |
| 模型参数量 | 5B | CogVideoX-5B 基础模型 |
| GPU | 8 × A100 | 推荐配置 |
